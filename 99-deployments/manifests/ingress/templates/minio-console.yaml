---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.minIoConsole.service.name }}
spec:
  ports:
    - port: {{ .Values.minIoConsole.service.port }}
      targetPort: {{ .Values.minIoConsole.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.minIoConsole.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.minIoConsole.endpoints.ip }}
    ports:
      - port: {{ .Values.minIoConsole.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.minIoConsole.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.minIoConsole.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.minIoConsole.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.minIoConsole.ingress.host }}
      secretName: {{ .Values.minIoConsole.ingress.name }}
  rules:
    - host: {{ .Values.minIoConsole.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.minIoConsole.service.name }}
                port:
                  number: {{ .Values.minIoConsole.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.minIoConsole.ingress.name }}
spec:
  secretName: {{ .Values.minIoConsole.ingress.name }}
  dnsNames:
    - {{ .Values.minIoConsole.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
