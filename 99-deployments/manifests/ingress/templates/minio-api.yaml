---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.minIoApi.service.name }}
spec:
  ports:
    - port: {{ .Values.minIoApi.service.port }}
      targetPort: {{ .Values.minIoApi.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.minIoApi.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.minIoApi.endpoints.ip }}
    ports:
      - port: {{ .Values.minIoApi.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.minIoApi.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.minIoApi.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.minIoApi.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.minIoApi.ingress.host }}
      secretName: {{ .Values.minIoApi.ingress.name }}
  rules:
    - host: {{ .Values.minIoApi.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.minIoApi.service.name }}
                port:
                  number: {{ .Values.minIoApi.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.minIoApi.ingress.name }}
spec:
  secretName: {{ .Values.minIoApi.ingress.name }}
  dnsNames:
    - {{ .Values.minIoApi.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
