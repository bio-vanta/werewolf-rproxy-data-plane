---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.pleasePaymentNotiHubProd.service.name }}
spec:
  ports:
    - port: {{ .Values.pleasePaymentNotiHubProd.service.port }}
      targetPort: {{ .Values.pleasePaymentNotiHubProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.pleasePaymentNotiHubProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.pleasePaymentNotiHubProd.endpoints.ip }}
    ports:
      - port: {{ .Values.pleasePaymentNotiHubProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.pleasePaymentNotiHubProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.pleasePaymentNotiHubProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.pleasePaymentNotiHubProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.pleasePaymentNotiHubProd.ingress.host }}
      secretName: {{ .Values.pleasePaymentNotiHubProd.ingress.name }}
  rules:
    - host: {{ .Values.pleasePaymentNotiHubProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.pleasePaymentNotiHubProd.service.name }}
                port:
                  number: {{ .Values.pleasePaymentNotiHubProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.pleasePaymentNotiHubProd.ingress.name }}
spec:
  secretName: {{ .Values.pleasePaymentNotiHubProd.ingress.name }}
  dnsNames:
    - {{ .Values.pleasePaymentNotiHubProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
